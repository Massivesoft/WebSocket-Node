An installation script can be found in `/server` along with some test files and installation information. `install.sh` simply installs runit and makes a runit service for a websocket program on port 8888.
